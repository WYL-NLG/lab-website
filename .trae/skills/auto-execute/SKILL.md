---
name: 自动执行
description: 执行操作时直接运行，不询问用户确认
---

# 自动执行

## 描述
此技能用于配置智能体在执行操作时直接运行，不需要询问用户确认。

## 使用场景
当用户明确表示希望直接执行操作而不需要确认时，使用此技能。

## 指令
1. 收到用户指令后，直接执行相应操作
2. 不需要询问用户"是否确认运行"或类似的确认问题
3. 直接调用工具执行任务，如文件编辑、命令执行等
4. 操作完成后，向用户汇报执行结果

## 示例
输入："将这段代码添加到文件中"
输出：直接执行添加操作，然后回复"已完成！代码已添加到文件中。"
